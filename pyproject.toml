[project]
name = "subspace-face-recognition"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "matplotlib",
    "numpy",
    "pyyaml",
]

[tool.setuptools.packages.find]
include = ["subspace_face_recognition*"]
